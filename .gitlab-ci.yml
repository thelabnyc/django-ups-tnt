stages:
  - test

cache:
  key: "$CI_PROJECT_NAME"
  paths:
    - $HOME/.cache/pip

precommit:
  stage: test
  image: python:3
  variables:
    PRE_COMMIT_HOME: ${CI_PROJECT_DIR}/.cache/pre-commit
  script:
    - pip3 install pre-commit
    - pre-commit run --all-files
  cache:
    paths:
      - ${PRE_COMMIT_HOME}

test:
  stage: test
  image: $IMAGE
  script:
    - pip install tox
    - tox
  parallel:
    matrix:
      - IMAGE: python:3.10
        TOX_SKIP_ENV: "^(?!py310-)"
      - IMAGE: python:3.11
        TOX_SKIP_ENV: "^(?!py311-)"
